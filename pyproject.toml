[project]
name = "blog"
version = "0.1.0"
description = "Technical blog on distributed systems, authentication, and protocol design"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material",
    "mkdocs-mermaid2-plugin",
    "mkdocs-panzoom-plugin",
]
